La ou tout les tests sont regroupés.
Pour en lancer un il faut executer: `pytest test_file.py` ou simplement `pytest` pour lancer tous les tests.

Necessite l'installation de pytest (`pip install pytest`)